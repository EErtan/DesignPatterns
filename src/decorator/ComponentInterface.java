package decorator;

public interface ComponentInterface{ // may also be an abstract class if you need the extended classes or if the need the state fields
	public int valueGet();
	public String nameGet();
}
